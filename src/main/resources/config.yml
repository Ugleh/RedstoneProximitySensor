config:
  update_checker: true
  use_sqlite: false
sensor_config:
  use_particles: true
  max_range: 20
  default_range: 5
  default_inverted: false
  default_owner_only_edit: true
  default_triggers:
    OWNER: true
    PLAYER_ENTITY: true
limiter: